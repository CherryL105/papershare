ELSEVIER_API_KEY=
# Required on first startup when the bootstrap admin user does not yet exist
PAPERSHARE_BOOTSTRAP_ADMIN_PASSWORD=
# Optional: override the runtime storage directory. Defaults to .local/storage
PAPERSHARE_STORAGE_DIR=
# Optional: override the port. Defaults to 3000
PORT=
